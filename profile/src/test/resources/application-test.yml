spring:
  main:
    banner-mode: off
    log-startup-info: false
    web-application-type: reactive

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: "http://localhost/disabled"
          jwk-set-uri: "http://localhost/oauth2/jwks"
      client:
        provider:
          spring:
            authorization-uri: "http://localhost/oauth2/authorize"
            token-uri: "http://localhost/oauth2/token"

  cloud:
    config:
      enabled: false
      import-check:
        enabled: false
    bootstrap:
      enabled: false

  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

  mail:
    username: "test@example.com"

springdoc:
  oAuthFlow:
    authorizationUrl: http://localhost/oauth2/authorize
    tokenUrl: http://localhost/oauth2/token

kafka-settings:
  topics:
    mail: sentMail

kafka:
  bootstrapAddress: localhost:9092

auth:
  server:
    revoke-uri: http://localhost/oauth2/revoke

catalog:
  service:
    products:
      get:
        uri: http://localhost/catalog/products

payment:
  service:
    charge:
      post:
        uri: http://localhost/payment/charge
    customer:
      get:
        uri: http://localhost/payment/customer

profile:
  service:
    get:
      uri: http://localhost/profile
    post:
      uri: http://localhost/profile

stripe:
  secret:
    key: sk_test_dummy

webClient:
  oauth2Client:
    defaultClientRegistrationId: test-client
  oath2Client:
    defaultClientRegistrationId: test-client

logging:
  level:
    root: ERROR
    com.ganchevdimitarg.profile: DEBUG

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false